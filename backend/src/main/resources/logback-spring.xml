<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Dev Profile: Console Appender with rich color formatting -->
    <springProfile name="dev">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %magenta([%thread]) %highlight(%-5level) %cyan(%logger{36}) - %msg%n</pattern>
                <charset>UTF-8</charset>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE" />
        </root>
        <logger name="com.back2kasi" level="DEBUG" />
    </springProfile>

    <!-- Prod Profile: Plain structured console output (best for container/cloud logging aggregation) -->
    <springProfile name="prod">
        <!-- Structured Console Appender (no colors for clean aggregation) -->
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
                <charset>UTF-8</charset>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE" />
        </root>
        <!-- Limit logs from noisy framework packages in production -->
        <logger name="org.springframework" level="WARN" />
        <logger name="org.hibernate" level="WARN" />
        <logger name="com.back2kasi" level="INFO" />
    </springProfile>

</configuration>
