package com.back2kasi.config;

/**
 * Springdoc OpenAPI configuration — STUBBED until the springdoc dependency is added.
 *
 * <p>To activate Swagger UI:</p>
 * <ol>
 *   <li>Add the dependency to {@code pom.xml}:
 *       {@code org.springdoc:springdoc-openapi-starter-webmvc-ui:2.5.0}</li>
 *   <li>Restore the full implementation from git history or re-generate from plan.</li>
 *   <li>The {@code SecurityConfig} already permits {@code /swagger-ui/**} and
 *       {@code /v3/api-docs/**} routes, so no SecurityConfig change is needed.</li>
 * </ol>
 *
 * <p>Swagger UI URL once active:
 * <a href="http://localhost:8080/swagger-ui.html">http://localhost:8080/swagger-ui.html</a></p>
 */
// @Configuration   — commented out until springdoc dependency is available
public class OpenApiConfig {
    // Full implementation requires springdoc-openapi-starter-webmvc-ui on the classpath.
    // See pom.xml TODO comment and the implementation plan for the full bean definition.
}
